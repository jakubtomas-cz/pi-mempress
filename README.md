## pi-mempress

A simple extension for pi.dev agent that displays current memory pressure and usage in the status line

### Installation

```bash
pi install git:github.com/jakubtomas-cz/pi-mempress@v0.1.0
```